# Google Workspace SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=admin@all4youauctions.co.za       # ← Your Google Workspace email
SMTP_PASS=depgaxebnkhkfaky                   # ← NEW App Password for workspace account
SMTP_FROM=admin@all4youauctions.co.za        # ← Same workspace email

# Application Settings
JWT_SECRET=supersecretkey

# Debug Settings (set to true for troubleshooting)
EMAIL_DEBUG=true
