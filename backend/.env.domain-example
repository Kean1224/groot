# Custom Domain SMTP Configuration (Alternative)
SMTP_HOST=mail.all4youauctions.co.za        # ← Your domain's mail server
SMTP_PORT=587                               # ← Check with your hosting provider
SMTP_USER=admin@all4youauctions.co.za       # ← Your domain email
SMTP_PASS=your-domain-email-password        # ← Regular password (not App Password)
SMTP_FROM=admin@all4youauctions.co.za

# OR if your hosting uses different settings:
# SMTP_HOST=smtp.yourhostingprovider.com
# SMTP_PORT=465 or 25
# SMTP_SECURE=true
